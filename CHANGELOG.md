# 更新日志(Changelog)
> 本项目所有版本改动记录，遵循 Keep a Changelog 规范。
> 版本号规则：主版本.次版本.补丁版本
> - 主版本：重大架构改动，不兼容旧协议
> - 次版本：新增功能
> - 补丁版本：Bug修复、细节优化

## [v0.1.1] - 2026-09-13
### ✨ 新增功能
1. **密保密码找回全套交互功能**
    - 客户端登录菜单新增选项 `3 找回密码`，独立的密码找回流程。
    - 新增指令 `getsecq|用户名`：查询目标用户预先设置的密保问题。
    - 新增指令 `resetpwd|用户名|密保答案|新密码`：校验密保答案，答案匹配则重置账号密码。
    - 配套新增设置密保指令 `setsecq|问题|答案`，用户登录后可预先设置自己的密保问答，用于后续找回密码。
2. **客户端 Ping/Pong 心跳保活机制**
    - 用户登录成功后自动启动独立心跳协程，**每3秒自动向服务端发送 ping 心跳包**。
    - 接收协程识别服务端返回的 `pong` 应答，更新最后存活时间戳 `lastPongAt`。
    - 超时检测逻辑：**连续10秒未收到pong应答，判定连接失效**，客户端主动打印心跳超时提示、关闭TCP连接并退出程序。
    - 心跳消息静默过滤：收到`pong`应答只更新计时，不再输出到聊天控制台，避免心跳包刷屏干扰正常聊天内容。

### 🐛 Bug 修复
1. **修复TCP SetWriteDeadline持久化超时BUG**
    - 问题描述：`SetWriteDeadline` 设置的超时会永久绑定TCP连接，心跳ping设置1s写超时后，超时不会自动清除。超过1秒后，后续所有聊天消息调用`Write`都会触发`i/o timeout`，造成第二个客户端上线一段时间后发送消息失败。
    - 解决方案：发送完ping包之后，调用`conn.SetWriteDeadline(time.Time{})`清空写超时，将心跳发包的超时和正常聊天消息的写超时做隔离，互不影响。

### 🔧 代码结构与交互优化
1. 重构客户端登录成功后的协程模型：采用双独立Goroutine，一个协程持续接收服务端推送消息，另一个协程专门负责定时心跳发包与超时检测。
2. 完善聊天面板帮助文案，新增密保设置指令`setsecq`的使用说明，所有可用指令一次性展示给用户，方便快速查阅。

---

## [v0.1.0] - 初始版本（已上传GitHub）
### ✨ 基础功能
1. TCP自定义通信协议：实现4字节大端序长度前缀编解码器 `Encode/Decode`，解决TCP粘包、分包问题。
2. 用户账号体系：支持账号注册、账号登录，使用SQLite数据库持久化存储账号与密码信息。
3. 好友管理系统：添加好友、查看收到的好友申请、同意好友申请、拒绝好友申请、查看好友列表、删除好友。
4. 消息模块
    - 公共广播消息：发送消息，所有在线客户端均可收到。
    - 点对点私聊：支持`@用户名 消息`格式私聊，**离线消息持久化存储**，对方上线后自动收到离线消息。
5. 服务端能力：监听`0.0.0.0:8080`端口，支持多客户端TCP并发连接，独立处理每个客户端会话。
### 📦 存储
- SQLite本地数据库，持久保存用户账号、好友关系、离线消息数据，重启服务数据不丢失。
